---
apiVersion: v1
kind: Service
metadata:
  name: app-prod-api-service
  labels:
    env: prod
spec:
  type: ClusterIP
  selector:
    app: app-prod-api
  ports:
  - name: flask-port
    port: 5000
    targetPort: 5000
